export declare type BreakpointType = 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';

export declare type CallableType = (event?: Event, target?: HTMLElement) => void | boolean;

export declare type ViewPortType = {
	width: number;
	height: number;
};

export declare type OffsetType = {
	top: number;
	left: number;
	right: number;
	bottom: number;
};

export declare type OptionType = string | number | boolean | object;
